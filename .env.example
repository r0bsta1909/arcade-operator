# Server env vars (Render). Never commit real values. GDD 5.5 / 5.6.
GITHUB_TOKEN=            # fine-grained token, Issues: Read and write, this repo only
GITHUB_REPO=r0bsta1909/arcade-operator
PHASE=friends            # friends | public
INVITE_CODES=code1,code2 # comma-separated, only checked in PHASE=friends
PORT=3000
